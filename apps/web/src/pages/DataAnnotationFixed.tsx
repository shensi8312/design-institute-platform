import { Card, Typography } from 'antd'

const DataAnnotationFixed: React.FC = () => (
  <Card>
    <Typography.Title level={4}>数据标注（内测版）</Typography.Title>
    <Typography.Paragraph type="secondary">
      内测版本暂时下线，请等待后续更新。
    </Typography.Paragraph>
  </Card>
)

export default DataAnnotationFixed
