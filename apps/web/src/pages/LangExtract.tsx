import { Card, Typography } from 'antd'

const LangExtract: React.FC = () => (
  <Card>
    <Typography.Title level={4}>LangExtract 服务</Typography.Title>
    <Typography.Paragraph type="secondary">
      LangExtract 集成正在维护中，暂不提供前端操作入口。
    </Typography.Paragraph>
  </Card>
)

export default LangExtract
