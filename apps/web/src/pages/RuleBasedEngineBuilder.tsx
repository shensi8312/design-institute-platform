import { Card, Typography } from 'antd'

const RuleBasedEngineBuilder: React.FC = () => (
  <Card>
    <Typography.Title level={4}>规则引擎构建器</Typography.Title>
    <Typography.Paragraph type="secondary">
      规则引擎构建器正在重构中，暂停使用。
    </Typography.Paragraph>
  </Card>
)

export default RuleBasedEngineBuilder
